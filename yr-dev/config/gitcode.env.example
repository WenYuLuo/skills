# Copy to config/gitcode.env.local or ~/.config/yr-dev/gitcode.env.
# Do not commit files containing real tokens.

GITCODE_API_BASE=https://gitcode.com/api/v5
GITCODE_TOKEN=

# Optional. If omitted, scripts use `git config user.name` and `git config user.email`.
YR_SIGNOFF_NAME=
YR_SIGNOFF_EMAIL=
